name: helloGithubAction

on: [push]

jobs:
  build:
    runs-on: ubnutu-latest
    steps:
     # run 뒤에는 실제 작동하는 코드를 넣어야 한다.
    - name: hello world 출력!!
      run: echo Hello, world
    - name: 디렉토리 출력!!
      run: ls -al
    - name: 파이썬 버전 출력!!
      run: python -v
